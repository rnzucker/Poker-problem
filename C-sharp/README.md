This is a C# version of Project Euler #54, the poker problem.
This first version works, but then I added one that used properties
(see sub-directory) to use setters and getters.
Still want to:
  1) Create a constant properly instead of the Global class
